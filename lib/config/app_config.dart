class AppConfig {
  AppConfig._();

  static const String appName = 'Drive Replay';
  static const bool isDevelopment = true;
  
  // Database configuration
  static const String dbName = 'drive_replay_db';
}
